#include <stdio.h>

/**
 * main - Print all single digit numbers of base 10 starting from zero.
 * Return: 0.
 */

int main(void)
{
int n = 48;
while (n <= 57)
{
putchar(n);
n += 1;
}
printf("\n");
return (0);
}
